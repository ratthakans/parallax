/* ── เครื่องมือเดโมที่ทำลายของได้ ต้องปิดบนที่สาธารณะ ──────────

   คอนโซลที่ /app เปิดให้ใครก็เข้าได้และไม่มีระบบล็อกอิน ซึ่งตั้งใจ —
   ตอนนี้ยังไม่มีลูกค้าสักราย มันจึงเป็นสิ่งเดียวบนเว็บที่ไม่ใช่คำพูด
   คนแปลกหน้ากดปุ่มแล้วเห็นเครื่องยนต์ทำงานจริงได้ทันที

   แต่หน้าตั้งค่ามีปุ่มสองตัวที่ทำลายสภาพนั้นได้จากภายนอก:

     Rebuild the dataset     ลบแคมเปญและผลวัดทั้งบัญชี
     Travel forward N days   เลื่อนวันอนุมัติทั้งชุดแล้ววัดใหม่

   ทั้งสองตัวมีไว้ให้คนพัฒนาใช้ ไม่ได้มีไว้ให้ผู้เยี่ยมชม ถ้ามีคนกด
   ตอนเช้า นักลงทุนที่เปิดดูตอนบ่ายจะเห็นเดโมที่ถูกล้างหรือถูกเลื่อนเวลา
   โดยไม่มีใครรู้ว่าเกิดอะไรขึ้น — ไม่ใช่ช่องโหว่ความปลอดภัยเพราะข้อมูล
   สังเคราะห์ทั้งหมด แต่เป็นความเสี่ยงต่อการขายซึ่งตอนนี้สำคัญกว่า

   ค่าตั้งต้น: เปิดตอนพัฒนา ปิดบน Vercel
   สั่งทับได้ด้วย PARALLAX_DEMO_TOOLS=1 หรือ 0

   ── สิ่งที่ยังแก้ไม่ได้ด้วยไฟล์นี้ ──

   ผู้เยี่ยมชมยังอนุมัติและส่งแคมเปญได้ ซึ่งก็เปลี่ยนสิ่งที่คนถัดไปเห็น
   เหมือนกัน นั่นคือการทำงานของเดโม ไม่ใช่บั๊ก — ทางแก้จริงคือให้แต่ละ
   ผู้เยี่ยมชมมีชุดข้อมูลของตัวเอง ซึ่งต้องรอย้ายฐานข้อมูลเสร็จก่อน */
export function demoToolsEnabled(): boolean {
  const flag = process.env.PARALLAX_DEMO_TOOLS;
  if (flag === "1") return true;
  if (flag === "0") return false;
  return !process.env.VERCEL;
}

export const DEMO_TOOLS_OFF_REASON =
  "Demo tools are turned off on the public console, so nobody can wipe or " +
  "time-shift the dataset other visitors are looking at. Set " +
  "PARALLAX_DEMO_TOOLS=1 to enable them.";
